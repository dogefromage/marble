import React from 'react';

export const MENU_PORTAL_MOUNT_ID = 'menu-portal-mount';

export const MenuPortalMount = () =>
{
    return (
        <div 
            id={MENU_PORTAL_MOUNT_ID}
        />
    );
}