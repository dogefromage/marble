import React from 'react';

export const CONTEXT_MENU_PORTAL_MOUNT_ID = 'context-menu-portal-mount';

export const ContextMenuPortalMount = () =>
{
    return (
        <div 
            id={CONTEXT_MENU_PORTAL_MOUNT_ID}
        />
    );
}